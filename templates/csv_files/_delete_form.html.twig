<form method="post" action="{{ path('app_csv_files_delete', {'id': csv_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ csv_file.id) }}">
    <button class="btn">Delete</button>
</form>
